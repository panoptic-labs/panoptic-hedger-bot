# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
dist/
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Bot key material (encrypted keystore written by `pnpm onboard`)
bot-keystore.json
*.keystore.json

# Interrupted-onboard resume state (sensitive even when encrypted)
deploy-state.json

# Per-machine runtime heartbeat + live-trading activation marker
.hedger-runtime.json
.hedger-activated.json
.hedger-journal.json
.hedger-disabled.json
.hedger-instance.lock
bot-keystore-passphrase

# Private operator recovery notes are never publication inputs. Keep them
# outside the repository; this ignore is immediate local containment only.
/RECOVERY.md

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
*.swp
*.swo
*~

# IDE
.vscode/
.idea/

# TypeScript
*.tsbuildinfo

# Misc
.cache/
